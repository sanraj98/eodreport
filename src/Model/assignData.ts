class assignData{
    id:number;
    assignedData:String;

    constructor(id: number, assignedData: String){
        this.id = id;
        this.assignedData= assignedData; 
    }
}

export default  assignData;